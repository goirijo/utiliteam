#include stuff

int main(){
    /*
    read in poscar
    generate factor group
    genreate subgroups
    for each subgroup:  
        find coset given subgroup and factor group
        find invariant subspace(1 wyckoff position)
        find all symmetrically equivalent (1 wyckoff*coset)
     print wyckoff position
        */
}
